[project]
name = "mistake-gravity-index"
version = "1.1.0"
description = "Decision-impact assistant coach using Mistake Gravity Index"
requires-python = ">=3.10"

dependencies = [
  "requests",
  "python-dotenv",
  "pandas",
  "numpy",
  "rich",
]

[tool.pytest.ini_options]
pythonpath = ["src"]